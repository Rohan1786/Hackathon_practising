<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
</head>
<body>
  <h1>Profile</h1>
  <h2>Welcome, <%= user.username %>!</h2>
  <h3>Email: <%= user.email %></h3>

  <h2>Order Records</h2>
  <% if (orderRecord) { %>
    <table border="1">
      <tr>
        <th>Username</th>
        <th>Order Count</th>
      </tr>
      <tr>
        <td><%= orderRecord.username %></td>
        <td><%= orderRecord.count %></td>
      </tr>
    </table>
  <% } else { %>
    <p>No order records found.</p>
  <% } %>
</body>
</html>
